import { {{Name}}, {{Name}}Document } from "./{{name}}.schema";
import { InjectModel } from "@nestjs/mongoose";
import { BaseService } from "base";
import { Model } from "mongoose";

export class {{Name}}Service extends BaseService<{{Name}}Document> {
  constructor(
    @InjectModel({{Name}}.name)
    private {{name}}Model: Model<{{Name}}Document>
  ) {
    super({{name}}Model)
  }
}
